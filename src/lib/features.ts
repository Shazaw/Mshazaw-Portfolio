/**
 * Sections that are built but deliberately switched off.
 *
 * The CTF area is finished and its schema, views, queries and seed are all
 * still in the tree — only its routes are parked, in `src/parked/ctf-routes`.
 * Flipping this back on plus moving that folder restores it; see
 * `src/parked/README.md`.
 */
export const CTF_ENABLED = false
